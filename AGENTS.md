# Tech Stack
This project uses React, Vite, Tailwind CSS, and shadcn.

# Rules
Use shadcn components instead of building components from scratch. Use the shadcn mcp to search for and install new components.

Use strict typescript types whenever possible. Fix type errors as you encounter them.

Always use the latest versions of libraries.